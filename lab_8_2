# Задаем размер матрицы
N = 4

# Создаем случайную матрицу A(N, N)
A = np.random.randint(1, 100, size=(N, N))

# Задаем номер строки m, с которой нужно переставить
m = 2  # Обратите внимание, что m должна быть в пределах от 0 до N-1

print("Исходная матрица:")
print(A)

# Находим индекс строки с максимальным элементом на главной диагонали
max_diag_index = np.argmax(np.diag(A))

# Переставляем строки, если они разные
if max_diag_index != m:
    A[[max_diag_index, m]] = A[[m, max_diag_index]]

print("Матрица после перестановки:")
print(A)
