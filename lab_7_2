# Ввод количества элементов
N = int(input("Введите количество элементов в массиве: "))
# Ввод элементов массива
arr = list(map(int, input("Введите элементы массива (через пробел): ").split()))

# Проверяем, если массив пуст
if N == 0:
    print("Количество различных элементов в массиве: 0")
else:
    unique_count = 1  # Так как первый элемент уникален
    
    # Перебираем элементы массива, начиная со второго
    for i in range(1, N):
        if arr[i] != arr[i - 1]:  # Если элемент отличается от предыдущего
            unique_count += 1  # Увеличиваем счетчик уникальных элементов

    print("Количество различных элементов в массиве:", unique_count)
